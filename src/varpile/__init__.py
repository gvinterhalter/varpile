# TODO: fix the comment (it fails if we are not in the correct directory
# import tomli
#
# with open("pyproject.toml", "rb") as file:
#     pyproject = tomli.load(file)
#     __VERSION__ = pyproject.get("project", {}).get("version")

__VERSION__ = "0.0.1"
